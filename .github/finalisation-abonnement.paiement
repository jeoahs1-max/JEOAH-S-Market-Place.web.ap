<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paiement - JEOAH'S</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f7f9fb; }
        .navbar-shadow { box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06); }
        .text-jeoahs { color: #032C4F; }
        .bg-jeoahs { background-color: #032C4F; }
        .payment-option {
            transition: all 0.2s;
            cursor: pointer;
            border: 3px solid transparent;
        }
        .payment-option:hover {
            transform: scale(1.02);
        }
        .payment-option.selected {
            border: 3px solid #032C4F;
            box-shadow: 0 0 15px rgba(3, 44, 79, 0.3);
            transform: scale(1.03);
        }
        /* Styles pour les boutons de paiement du bas (Maquette) */
        .btn-paypal { background-color: #FFC439; color: #103456; }
        .btn-googlepay { background-color: #000000; color: #FFFFFF; }
        .btn-alipay { background-color: #1677FF; color: #FFFFFF; }
        .btn-moncash { background-color: #E63D33; color: #FFFFFF; }
        .btn-cashapp { background-color: #00D54B; color: #FFFFFF; }
        .btn-natcash { background-color: #6C5CE7; color: #FFFFFF; }
    </style>
</head>
<body>

    <!-- Modal pour alert() -->
    <div id="custom-modal" class="fixed inset-0 bg-gray-900 bg-opacity-50 hidden z-50 flex items-center justify-center p-4">
        <div class="bg-white p-6 rounded-lg shadow-xl max-w-sm w-full">
            <h4 id="modal-title" class="text-xl font-bold mb-3 text-jeoahs"></h4>
            <p id="modal-content" class="text-gray-700 mb-4"></p>
            <button id="modal-confirm" class="w-full py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition" onclick="document.getElementById('custom-modal').classList.add('hidden')">
                Fermer
            </button>
        </div>
    </div>

    <!-- Barre de Navigation (Header) -->
    <header class="bg-white navbar-shadow sticky top-0 z-40">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
            <!-- Logo/Nom -->
            <a href="index.html" class="text-2xl font-extrabold text-jeoahs tracking-tight">
                JEOAH'S Market Place
            </a>

            <!-- Navigation Principale -->
            <nav class="hidden md:flex space-x-6 lg:space-x-10 items-center">
                <a href="index.html" class="text-gray-600 hover:text-jeoahs transition pb-1">Accueil</a>
                <a href="fonctionnalites.html" class="text-gray-600 hover:text-jeoahs transition pb-1">Fonctionnalités</a>
                <a href="plans.html" class="text-gray-900 font-semibold hover:text-jeoahs transition border-b-2 border-jeoahs pb-1">Plans & Tarifs</a>
                <a href="contact.html" class="text-gray-600 hover:text-jeoahs transition pb-1">Contact</a>
                
                <!-- Sélecteur de Langue (simulé) -->
                <div class="relative">
                    <select id="lang-selector" class="py-1 px-2 border border-gray-300 rounded-lg text-sm text-gray-700 focus:ring-blue-500 focus:border-blue-500">
                        <option value="fr">🇫🇷 FR</option>
                        <option value="en">🇺🇸 EN</option>
                        <option value="es">🇪🇸 ES</option>
                    </select>
                </div>
            </nav>

            <!-- Bouton de Connexion/Déconnexion pour mobile et desktop -->
            <div class="flex items-center space-x-3">
                <span id="user-status" class="text-sm text-gray-500 hidden lg:block"></span>
                <button id="auth-button" class="bg-jeoahs text-white font-semibold py-2 px-4 rounded-lg shadow-md hover:bg-[#021f38] transition">
                    Se connecter
                </button>
            </div>
        </div>
    </header>

    <!-- Contenu Principal -->
    <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12 md:py-20">
        <header class="text-center mb-10">
            <h1 class="text-5xl font-extrabold text-jeoahs mb-4">
                Finaliser votre Abonnement
            </h1>
            <p class="text-xl text-gray-600">
                Veuillez vérifier votre commande avant de payer.
            </p>
        </header>

        <!-- Détails de la Commande -->
        <div class="bg-white p-6 sm:p-8 rounded-xl shadow-lg mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-4 border-b pb-2">Détails de la Commande</h2>
            
            <div id="order-details">
                <p class="text-gray-500 text-center py-4">Chargement des détails de votre plan...</p>
            </div>

            <div class="flex justify-between items-center pt-4 border-t mt-4">
                <span class="text-xl font-bold text-jeoahs">TOTAL À PAYER</span>
                <span id="total-price" class="text-2xl font-extrabold text-jeoahs">$0.00</span>
            </div>
        </div>

        <!-- Choix du Moyen de Paiement (Maquette fournie) -->
        <div class="bg-white p-6 sm:p-8 rounded-xl shadow-lg mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">Choisissez votre moyen de paiement</h2>
            
            <div class="grid grid-cols-2 gap-4">
                
                <button type="button" data-method="paypal" class="payment-option btn-paypal py-4 px-4 rounded-xl font-bold text-lg shadow-md hover:shadow-lg">
                    <i class="fab fa-paypal mr-2"></i> PayPal
                </button>
                
                <button type="button" data-method="googlepay" class="payment-option btn-googlepay py-4 px-4 rounded-xl font-bold text-lg shadow-md hover:shadow-lg">
                    <i class="fab fa-google-pay mr-2"></i> Google Pay
                </button>

                <button type="button" data-method="alipay" class="payment-option btn-alipay py-4 px-4 rounded-xl font-bold text-lg shadow-md hover:shadow-lg">
                    <i class="fas fa-wallet mr-2"></i> Alipay
                </button>

                <button type="button" data-method="moncash" class="payment-option btn-moncash py-4 px-4 rounded-xl font-bold text-lg shadow-md hover:shadow-lg">
                    <i class="fas fa-mobile-alt mr-2"></i> MonCash
                </button>
                
                <button type="button" data-method="cashapp" class="payment-option btn-cashapp py-4 px-4 rounded-xl font-bold text-lg shadow-md hover:shadow-lg">
                    <i class="fas fa-dollar-sign mr-2"></i> Cash App
                </button>

                <button type="button" data-method="natcash" class="payment-option btn-natcash py-4 px-4 rounded-xl font-bold text-lg shadow-md hover:shadow-lg">
                    <i class="fas fa-credit-card mr-2"></i> NatCash
                </button>
            </div>
        </div>

        <!-- Bouton Final -->
        <button id="confirm-payment-btn" disabled class="w-full py-4 px-6 bg-gray-400 text-white font-bold rounded-xl shadow-lg transition" onclick="processPayment()">
            Sélectionnez un moyen de paiement
        </button>

    </main>

    <!-- Footer (Repris de index.html) -->
    <footer class="bg-jeoahs text-white py-12">
        <!-- Contenu du Footer est le même que index.html et fonctionnalités.html -->
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid grid-cols-2 md:grid-cols-5 gap-8 border-b border-gray-700 pb-10 mb-8">
                
                <div class="col-span-2 md:col-span-1">
                    <h3 class="text-2xl font-extrabold mb-4">JEOAH'S</h3>
                    <p class="text-sm text-gray-400 mb-4">Votre plateforme d'e-commerce innovante et centralisée.</p>
                    <button id="pwa-install-btn-ft" class="flex items-center py-2 px-4 bg-red-600 text-white text-sm font-semibold rounded-lg hover:bg-red-700 transition">
                        <i class="fas fa-mobile-alt mr-2"></i> Installer l'App
                    </button>
                </div>
                
                <div>
                    <h4 class="text-lg font-bold mb-4">Navigation Rapide</h4>
                    <ul class="space-y-2">
                        <li><a href="index.html" class="text-gray-400 hover:text-white transition text-sm">Accueil</a></li>
                        <li><a href="fonctionnalites.html" class="text-gray-400 hover:text-white transition text-sm">Fonctionnalités</a></li>
                        <li><a href="contact.html" class="text-gray-400 hover:text-white transition text-sm">Contact</a></li>
                        <li><a href="plans.html" class="text-gray-400 hover:text-white transition text-sm">Plans & Tarifs</a></li>
                    </ul>
                </div>

                <div>
                    <h4 class="text-lg font-bold mb-4">Légal & Assistance</h4>
                    <ul class="space-y-2">
                        <li><a href="#" class="text-gray-400 hover:text-white transition text-sm">Politique de Confidentialité</a></li>
                        <li><a href="#" class="text-gray-400 hover:text-white transition text-sm">Termes & Conditions</a></li>
                        <li><a href="#" class="text-gray-400 hover:text-white transition text-sm">Assistance (Ney)</a></li>
                    </ul>
                </div>
                
                <!-- Réseaux Sociaux -->
                <div>
                    <h4 class="text-lg font-bold mb-4">Réseaux Sociaux</h4>
                    <p class="text-sm text-gray-400 mb-3">Suivez-nous sur :</p>
                    <div class="flex flex-wrap gap-3 text-2xl">
                        <i class="fab fa-twitter text-gray-400"></i>
                        <i class="fab fa-facebook text-gray-400"></i>
                        <i class="fab fa-youtube text-gray-400"></i>
                        <i class="fab fa-instagram text-gray-400"></i>
                        <i class="fab fa-linkedin text-gray-400"></i>
                        <i class="fab fa-reddit text-gray-400"></i>
                        <i class="fab fa-twitch text-gray-400"></i>
                        <i class="fab fa-telegram text-gray-400"></i>
                        <i class="fab fa-whatsapp text-gray-400"></i>
                    </div>
                </div>

                <!-- Modes de Paiement -->
                <div>
                    <h4 class="text-lg font-bold mb-4">Modes de Paiement</h4>
                    <p class="text-sm text-gray-400 mb-3">Nous acceptons :</p>
                    <div class="flex flex-wrap gap-2">
                        <span class="bg-yellow-500 text-gray-900 px-1 py-0.5 rounded-sm text-xs font-semibold">PayPal</span>
                        <span class="bg-black text-white px-1 py-0.5 rounded-sm text-xs font-semibold">G Pay</span>
                        <span class="bg-blue-600 text-white px-1 py-0.5 rounded-sm text-xs font-semibold">Alipay</span>
                        <span class="bg-red-600 text-white px-1 py-0.5 rounded-sm text-xs font-semibold">MonCash</span>
                        <span class="bg-purple-600 text-white px-1 py-0.5 rounded-sm text-xs font-semibold">NatCash</span>
                        <span class="bg-green-600 text-white px-1 py-0.5 rounded-sm text-xs font-semibold">Cash App</span>
                        <span class="bg-gray-300 text-gray-800 px-1 py-0.5 rounded-sm text-xs font-semibold">VISA</span>
                        <span class="bg-gray-300 text-gray-800 px-1 py-0.5 rounded-sm text-xs font-semibold">Stripe</span>
                    </div>
                </div>

            </div>

            <div class="text-center text-sm text-gray-500">
                &copy; 2025 JEOAH'S Marketplace. Tous droits réservés.
            </div>
        </div>
    </footer>

    <script type="module">
        // Import des librairies Firebase
        import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
        import { getAuth, signInAnonymously, signInWithCustomToken, onAuthStateChanged, signOut } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
        import { getFirestore, doc, updateDoc, setDoc } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";
        import { setLogLevel } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";

        // Variables globales Firebase
        const appId = typeof __app_id !== 'undefined' ? __app_id : 'default-app-id';
        const firebaseConfig = typeof __firebase_config !== 'undefined' ? JSON.parse(__firebase_config) : {};
        const initialAuthToken = typeof __initial_auth_token !== 'undefined' ? __initial_auth_token : null;
        
        let auth;
        let db;
        let currentUserId = null;
        let selectedPlan = null;
        let selectedMethod = null;

        // Données des plans (pour la démo)
        const PLANS_DATA = {
            'mensuel': { id: 'mensuel', name: 'Mensuel', price: 19.99, duration: 'mois' },
            'trimestriel': { id: 'trimestriel', name: 'Trimestriel', price: 59.97, duration: '3 mois' },
            'semestriel': { id: 'semestriel', name: 'Semestriel', price: 119.94, duration: '6 mois' },
            'annuel': { id: 'annuel', name: 'Annuel', price: 250.00, duration: 'an' }
        };

        // --- Fonctions d'aide pour l'UI (Modal) ---
        function showCustomModal(title, content) {
            document.getElementById('modal-title').textContent = title;
            document.getElementById('modal-content').textContent = content;
            document.getElementById('custom-modal').classList.remove('hidden');
        }

        // --- Logique d'Authentification ---
        function updateAuthUI(user) {
            const authButton = document.getElementById('auth-button');
            const userStatus = document.getElementById('user-status');
            currentUserId = user ? user.uid : null;
            if (user && user.uid) {
                authButton.textContent = 'Déconnexion';
                authButton.onclick = async () => { await signOut(auth); showCustomModal("Déconnexion", "Vous avez été déconnecté."); };
                authButton.classList.replace('bg-jeoahs', 'bg-red-600');
                userStatus.textContent = `Connecté: ${user.uid.substring(0, 8)}...`;
            } else {
                authButton.textContent = 'Se connecter';
                authButton.onclick = () => { window.location.href = 'auth.html'; };
                authButton.classList.replace('bg-red-600', 'bg-jeoahs');
                userStatus.textContent = `Non connecté.`;
            }
        }
        async function initAuth() {
            if (Object.keys(firebaseConfig).length === 0) { return updateAuthUI(null); }
            setLogLevel('Debug');
            const app = initializeApp(firebaseConfig);
            auth = getAuth(app);
            db = getFirestore(app);
            try {
                if (initialAuthToken) { await signInWithCustomToken(auth, initialAuthToken); } else { await signInAnonymously(auth); }
            } catch (error) { console.error("Erreur d'authentification initiale:", error); }
            onAuthStateChanged(auth, updateAuthUI);
        }

        // --- Logique de Paiement ---

        function renderOrderDetails(plan) {
            const detailsDiv = document.getElementById('order-details');
            const totalPriceSpan = document.getElementById('total-price');
            
            if (!plan) {
                detailsDiv.innerHTML = `
                    <div class="bg-red-100 p-4 rounded-lg text-red-800">
                        <i class="fas fa-exclamation-triangle mr-2"></i> Le plan sélectionné est invalide ou manquant. Retournez à <a href="plans.html" class="font-bold underline">Plans & Tarifs</a>.
                    </div>
                `;
                totalPriceSpan.textContent = '$0.00';
                return;
            }

            detailsDiv.innerHTML = `
                <div class="flex justify-between text-gray-700 mb-2">
                    <span>Abonnement : ${plan.name}</span>
                    <span>$${plan.price.toFixed(2)}</span>
                </div>
                <div class="flex justify-between text-gray-700 mb-2">
                    <span>Période : par ${plan.duration}</span>
                    <span>(Taxes incluses)</span>
                </div>
            `;
            totalPriceSpan.textContent = `$${plan.price.toFixed(2)}`;
            selectedPlan = plan;
        }

        function handlePaymentMethodSelection() {
            document.querySelectorAll('.payment-option').forEach(button => {
                button.addEventListener('click', function() {
                    // Désélectionner tous les autres
                    document.querySelectorAll('.payment-option').forEach(btn => btn.classList.remove('selected'));
                    
                    // Sélectionner celui-ci
                    this.classList.add('selected');
                    selectedMethod = this.getAttribute('data-method');
                    
                    // Activer le bouton de confirmation
                    const confirmBtn = document.getElementById('confirm-payment-btn');
                    confirmBtn.disabled = false;
                    confirmBtn.textContent = `Confirmer et Payer $${selectedPlan.price.toFixed(2)} avec ${selectedMethod.toUpperCase()}`;
                    confirmBtn.classList.remove('bg-gray-400');
                    confirmBtn.classList.add('bg-blue-600', 'hover:bg-blue-700');
                });
            });
        }
        
        async function processPayment() {
            if (!selectedPlan || !selectedMethod) {
                return showCustomModal('Erreur', 'Veuillez sélectionner un plan et un moyen de paiement.');
            }
            if (!currentUserId) {
                 return showCustomModal('Authentification Requise', 'Veuillez vous connecter avant de finaliser le paiement. Redirection vers la page de connexion...');
                 // Simuler la redirection
                 setTimeout(() => window.location.href = 'auth.html', 2000);
            }

            const confirmBtn = document.getElementById('confirm-payment-btn');
            confirmBtn.disabled = true;
            confirmBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i> Traitement...';

            // Path: /artifacts/{appId}/users/{userId}/user_data/profile
            const profileDocRef = doc(db, `artifacts/${appId}/users/${currentUserId}/user_data/profile`);
            
            try {
                // Créer ou mettre à jour le profil avec les données d'abonnement
                const updateData = {
                    subscriptionStatus: `${selectedPlan.name} Actif`,
                    subscriptionPlanId: selectedPlan.id,
                    subscriptionPrice: selectedPlan.price,
                    paymentMethod: selectedMethod,
                    subscriptionStartDate: new Date(),
                    lastUpdate: new Date()
                };

                await setDoc(profileDocRef, updateData, { merge: true });

                showCustomModal('Paiement Réussi !', `Félicitations ! Votre abonnement au plan ${selectedPlan.name} a été activé avec succès via ${selectedMethod.toUpperCase()}. Vous pouvez maintenant profiter de toutes les fonctionnalités.`);

                // Rediriger vers l'accueil après un court délai
                setTimeout(() => window.location.href = 'index.html', 3000);

            } catch (error) {
                console.error("Erreur lors de la finalisation de l'abonnement:", error);
                showCustomModal('Erreur Critique', "Une erreur est survenue lors de l'activation de l'abonnement. Veuillez réessayer ou contacter le support.");
            } finally {
                confirmBtn.disabled = false;
                confirmBtn.innerHTML = 'Confirmer et Payer';
            }
        }

        // Fonction principale de chargement
        function loadPage() {
            // 1. Récupérer le plan depuis l'URL
            const urlParams = new URLSearchParams(window.location.search);
            const planId = urlParams.get('plan');
            const plan = PLANS_DATA[planId];
            
            renderOrderDetails(plan);
            handlePaymentMethodSelection();
        }

        // Démarrage de l'application
        document.addEventListener('DOMContentLoaded', () => {
            initAuth();
            loadPage();
        });

    </script>
</body>
</html>

